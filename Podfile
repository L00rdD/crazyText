# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CrazyText' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CrazyText
  pod 'CSV.swift', '~> 2.1.0'
  pod 'CSVImporter', '~> 1.7'
  target 'CrazyTextTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CrazyTextUITests' do
    # Pods for testing
  end

end
